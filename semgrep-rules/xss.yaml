rules:
  - id: test-echo
    pattern: echo $VAR;
    message: echo kullanımı
    severity: WARNING
    languages: [php]

  - id: test-var-dump
    pattern: var_dump($VAR);
    message: var_dump kullanımı
    severity: WARNING
    languages: [php]
